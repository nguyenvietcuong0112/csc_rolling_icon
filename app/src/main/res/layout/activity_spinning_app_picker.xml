<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/cosmic_background"
    android:fitsSystemWindows="false">

    <!-- Cosmic Header Container -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_header"
        android:paddingBottom="16dp">

        <!-- Top Title Bar -->
        <RelativeLayout
            android:id="@+id/layoutTitleBar"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:paddingTop="24dp"
            android:paddingHorizontal="16dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_alignParentLeft="true"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_arrow_back"
                android:background="@drawable/bg_white_rounded_12"
                android:padding="10dp"
                android:tint="@color/cosmic_text_primary"
                android:clickable="true"
                android:focusable="true" />

            <TextView
                android:id="@+id/txtHeaderTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="@string/spinning_style_title"
                android:textColor="@color/cosmic_white"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="@font/app_font" />
        </RelativeLayout>
    </RelativeLayout>

    <!-- Selection Info and Clear Action Row -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp">

        <TextView
            android:id="@+id/txtSelectedCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:text="Selected: 0"
            android:textColor="@color/cosmic_text_subtitle"
            android:textSize="13sp"
            android:textStyle="bold"
            android:fontFamily="@font/app_font" />

        <ImageView
            android:id="@+id/btnClearSelection"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:src="@drawable/photo_icon_edit_close_bt"
            android:tint="#FF5959"
            android:padding="6dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true" />
    </RelativeLayout>

    <!-- Main Grid Recycler and Lottie Loading Container -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/appGridRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="6dp"
            android:clipToPadding="false"
            android:scrollbars="vertical" />

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/progressBar"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:visibility="gone"
            app:lottie_rawRes="@raw/loading_data"
            app:lottie_autoPlay="true"
            app:lottie_loop="true" />
    </FrameLayout>

    <!-- Horizontal Selected Items View and Next Action Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/cosmic_background">

        <!-- Selected Horizontal Recycler -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/selectedAppsHorizontalRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:clipToPadding="false"
            android:background="@color/cosmic_bg_light_lavender" />

        <!-- Continue Button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnNext"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:text="@string/btn_continue"
                android:textColor="@color/cosmic_white"
                android:background="@drawable/bg_purple_gradient_btn"
            android:stateListAnimator="@null"
                android:textSize="15sp"
                android:textStyle="bold"
                android:fontFamily="@font/app_font" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>


